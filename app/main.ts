import {Answer} from "./answer";
import {WonderWoman} from "wonderwoman";
import {Batman} from "batman";
var myAnswer = new Answer;
console.log( `The Answer is ${myAnswer.ultimateAnswer}` );
console.log(`${WonderWoman.name}'s Secret Identity is: '${WonderWoman.secretIdentity}'`);
console.log(`${Batman.name}'s Secret Identity is: '${Batman.secretIdentity}'`);
